# Clave pública de MercadoPago, utilizada en el frontend para inicializar el SDK.
# Comienza con "APP_USR-" en producción o "TEST-" en modo sandbox.
VITE_MP_PUBLIC="APP_USR-your_public_key_here"

# Clave de API de Google Maps, utilizada para renderizar mapas en el frontend.
VITE_MAPS="google-maps-key"

# Dominio público temporal.
# No incluir el protocolo (http/https), solo el dominio base. Quitar "/" del final de la URL.
NGROK="your-ngrok-url"

# URL local del backend. Quitar "/" del final de la URL.
VITE_BACKEND_URL="http://localhost:8000"
